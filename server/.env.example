# Slack Token Server Configuration

# Required: 64-character hex string (32 bytes) for encrypting stored tokens
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# Optional: Database URL (default: sqlite3 ./tokens.db)
# Examples:
#   SQLite: sqlite3://./tokens.db or file:./tokens.db
#   PostgreSQL: postgres://user:pass@localhost/dbname?sslmode=disable
DATABASE_URL=

# Optional: Server port (default: 8080)
PORT=8080
